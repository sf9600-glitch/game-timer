# 遊戲計時器 — 新手使用教學

歡迎使用本計時器。這份教學假設你是第一次打開頁面，會帶你從「建立帳號與標籤」到「啟動第一個倒數計時」，並說明進階功能。

---

## 這個 App 能做什麼？

- 為**多個遊戲帳號**（例如不同伺服器、不同角色）同時管理倒數計時。
- 每個計時可綁定**任務標籤**（如掛機、活動、副本）與**子項目**（如具體地圖或隊伍名）。
- 時間快到時，卡片會**紅色閃爍預警**。
- 時間到了會移到**已完成**區，顯示結束時間與已過多久。
- 支援**復原**誤刪、**匯出／匯入備份**，以及（若已設定）**雲端同步**多裝置共用。

---

## 畫面長什麼樣？

| 區域 | 說明 |
|------|------|
| **主畫面（右側／全螢幕）** | 依「帳號」分組，顯示進行中與已完成的計時卡片。 |
| **設定面板（左側）** | 管理帳號、標籤、啟動任務、系統與主題。電腦上通常一直開著；手機上要點「設定」才會滑出。 |

設定面板內有四個可收合區塊（點標題可展開／收合）：

1. **帳號管理** — 遊戲帳號與底下的角色  
2. **編輯任務標籤** — 任務種類（可含子標籤）  
3. **任務啟動** — 選帳號、設時間、開始計時  
4. **系統與主題** — 備份、復原、語言、外觀、雲端  

---

## 五分鐘快速上手

### 步驟 1：打開設定面板

- **電腦**：左側應已顯示設定面板。  
- **手機**：點第一個帳號標題列中央的 **「設定」**，或從左側滑出的 **「‹ 收起」** 可關閉面板；點畫面暗色區域也會關閉。

### 步驟 2：新增遊戲帳號

1. 展開 **「帳號管理」**。  
2. 在「帳號名稱」輸入框輸入名稱（例如 `sf9600`），按 **「新增」**。  
3. （選用）在帳號卡片內新增**角色名**（例如 `VINCE`），方便之後啟動任務時區分。  
4. （選用）點帳號或角色旁的小色塊可改顏色，主畫面上會用來區分。

> **改名**：點名稱下方虛線文字可重新命名。  
> **刪除**：點 **×**；刪除帳號會一併移除該帳號下所有進行中的計時器（會先出現確認視窗）。

### 步驟 3：建立任務標籤

1. 展開 **「編輯任務標籤」**。  
2. 輸入標籤名稱（例如 `掛機`），按 **「新增」**。  
3. 點標籤卡片可展開，在底下加**子項目**（例如 `森林`、`礦坑`），按 **+** 加入。  
4. 標籤可**拖拉排序**；子項目也可拖拉調整順序。

### 步驟 4：設定時間並啟動

1. 展開 **「任務啟動」**。  
2. 選 **帳號**、**角色**、**任務標籤**；若該標籤有子項目，會多一個下拉選單。  
3. 設定倒數時間，可用以下任一方式：  
   - **D / H / M / S** 四格直接輸入（天、時、分、秒）  
   - 快捷鍵：**+1d、+12h、+1h、+10m、+1m、+30s**  
   - 上方大字 **00:00:00** 會同步顯示總時長（時:分:秒）  
   - 數字變動時，**只有有改到的欄位會短暫放大閃爍**，方便用餘光確認  
4. 設定 **「預警閃爍」**（預設常為 `0:30`）：代表剩餘時間進入這個區間時，卡片開始紅色閃爍。  
5. 按 **「啟動任務」**。  

啟動後，主畫面該帳號下會出現計時卡片；手機上通常會自動收起設定面板，方便你看倒數。

### 步驟 5：看結果

- **進行中**：卡片顯示剩餘時間、預計結束日期；進度條會隨時間變色。  
- **預警中**：進入你設定的「預警閃爍」時間後，整張卡紅色脈動。  
- **已完成**：時間到後移到「已完成」區，顯示結束時間與「已過多久」。  
- **刪除單一計時**：點卡片右上角 **×**。  

---

## 帳號管理（詳細）

| 操作 | 做法 |
|------|------|
| 新增帳號 | 輸入名稱 → **新增** |
| 新增角色 | 在帳號卡片內輸入角色名 → **+** |
| 改名 | 點虛線底線的名稱 |
| 改顏色 | 點名稱旁色塊 |
| 刪除 | 點 **×** |
| 區塊顏色 | 點「帳號管理」標題右側色塊，可改整個區塊主題色 |

每個遊戲帳號在主畫面是一個**分頁式區塊**，底下再分「進行中」「已完成」。

---

## 編輯任務標籤（詳細）

標籤代表「任務類型」，例如：掛機、活動、採集。

| 操作 | 做法 |
|------|------|
| 新增標籤 | 輸入名稱 → **新增** |
| 子項目 | 點標籤卡片展開 → 輸入後按 **+** |
| 改名 | 點標籤或子項目名稱（虛線） |
| 改顏色 | 標籤右側色塊 |
| 排序 | 長按／拖拉標籤或子項目 |
| 刪除 | **×** |
| 一次展開全部 | 標籤區標題旁的按鈕（展開時才會出現） |

**預設標籤（進階）**

- **★ 將目前標籤設為預設**：把現在「編輯任務標籤」的內容存成你的個人預設。  
- **↺ 還原預設標籤**：恢復成你上次儲存的預設（不會刪帳號與進行中計時器；可用復原還原這步）。

---

## 任務啟動（詳細）

### 時間怎麼算？

- **D/H/M/S** 與快捷鍵都會改同一個「總秒數」。  
- 大字顯示的是 **時:分:秒**（不含天數；例如 1 天會顯示為 24:00:00）。  
- **重置時間** 會把所有時間清成 0。

### 預警閃爍

格式為 **分:秒**（例如 `0:30` = 剩 30 秒開始閃、`5:0` = 剩 5 分鐘開始閃）。

### 啟動後

- 時間歸零，可立刻設下一個任務。  
- 同一帳號可同時有多個進行中計時器。

### 清除已完成

- **「清除已完成計時器」**：一次移除所有「時間已到」的卡片。  
- 有已完成項目時，按鈕會特別標示提醒你。  
- 清除後可用畫面右下 **↩ 復原** 救回（在倒數秒數內）。

---

## 主畫面計時卡片

每張卡片包含：

- **#編號** — 該帳號下的序號  
- **角色徽章** — 有設角色才顯示  
- **任務名稱** — 含子項目時會顯示為 `標籤名 (子項目)`  
- **剩餘時間** — 大字倒數  
- **結束日期提示** — 例如「3/15 (2小時後)」  

**已完成**卡片改顯示：

- 結束時間（月/日 時:分）  
- 已過多久（例如「已過 2 小時」）  

---

## 復原（Undo）

刪除計時器、清除已完成、刪除帳號／標籤等操作後，右下角會出現 **↩ 復原** 提示。

- 預設約 **10 秒**內可復原（可在 **系統與主題 → 系統 → 復原顯示時間** 調整，1～120 秒）。  
- 點 **↩** 執行復原；點 **×** 關閉提示並放棄復原。  
- 可連續復原多步（會顯示「等 N 項」）。

---

## 系統與主題

### 系統

| 功能 | 說明 |
|------|------|
| **☁ 雲端同步** | 登入後，帳號、標籤、進行中計時器會同步到雲端；手機與電腦登入同一帳號可共用。（需依 `DEPLOY.md` 設定 Supabase） |
| **立即同步** | 手動比對雲端與本機，採較新的一方 |
| **匯出（完整備份）** | 下載 JSON 檔，含所有設定 |
| **匯入** | 從 JSON 還原（會覆蓋目前本機設定，請先匯出備份） |

### 主題

| 選項 | 說明 |
|------|------|
| **介面語言** | 繁體中文 / 简体中文 |
| **外觀模式** | 淺色、深色、自動（跟隨系統） |
| **深色霓虹光暈** | 僅深色／自動深色時，卡片與面板會有霓虹邊光（可關閉） |

---

## 手機版操作提示

| 情境 | 做法 |
|------|------|
| 打開設定 | 點第一個帳號列的 **「設定」** |
| 關閉設定 | **‹ 收起**、或點主畫面暗色區 |
| 沒有計時器時 | 首次進入可能自動打開設定面板，方便你建立第一個任務 |
| 啟動任務後 | 面板常會自動關閉，專心看倒數 |

---

## 常見問題

**Q：按了「啟動任務」沒反應？**  
A：請確認時間大於 0（不能是 00:00:00）。

**Q：為什麼有些計時器在「已完成」但還在列表裡？**  
A：時間到了會自動歸類為已完成，需按 **「清除已完成計時器」** 才會從畫面移除（或個別點 × 刪除）。

**Q：刪錯了怎麼辦？**  
A：看右下角是否有 **↩ 復原**，在倒數結束前點下去。

**Q：換手機資料不見？**  
A：若未登入雲端，資料只存在該瀏覽器的本機。請用 **匯出備份** 轉移，或設定雲端同步後登入同一帳號。

**Q：預警沒閃？**  
A：檢查「預警閃爍」是否設太短或為 0:0；並確認計時器仍在「進行中」且尚未結束。

**Q：深色模式想低調一點？**  
A：**系統與主題 → 主題 → 深色霓虹光暈 → 關閉**。

---

## 建議的日常流程

1. **第一次**：建立帳號 → 建立常用標籤 → （選用）設為預設標籤。  
2. **每次開遊戲**：打開設定 → 任務啟動 → 選帳號／角色／標籤 → 設時間 → 啟動。  
3. **活動結束**：到主畫面確認；要整理畫面時按 **清除已完成**。  
4. **換裝置前**：**匯出備份** 或確認雲端已登入並同步。  

---

## 需要技術設定時

- 本機測試：在專案資料夾執行 `python3 -m http.server 8765`，瀏覽器開 `http://localhost:8765`。  
- 上線與雲端：請參考同目錄的 `DEPLOY.md`、`AUTO_DEPLOY.md`。

---

在 App 內可從 **系統與主題 → 系統 → 📖 新手教學** 開啟本教學。
