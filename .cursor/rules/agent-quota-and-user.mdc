---
description: 省額度 + 非程式背景用戶：繁中白話、小範圍改動、三句收尾
alwaysApply: true
---

# Agent 工作方式（本專案預設）

用戶**不懂程式**，以 **Agent 改程式**為主。請節省輪次與上下文，並用**繁體中文白話**溝通。

## 改動原則

- **只改與本次需求有關的檔**，不重構整專案、不順便改無關功能。
- 專案已拆分：`index.html`（結構）、`css/app.css`（樣式）、`js/app.js`（邏輯與 Supabase）。**優先改 css/js**，除非必須才動 `index.html`。
- **同一問題盡量一次改完**；若需確認，**最多一個**關鍵選擇題，不要長篇教學或逐行講解語法。
- 列表／卡片版面可參考 `docs/文字列表版面-提示詞範本.md`（左側整組、時間欄對齊）。

## 收尾（每次實作結束必須有）

用**三句話**說明：
1. 改了什麼（白話）
2. 要 **Cmd+S** 存哪幾個檔（通常是 `css/app.css`、`js/app.js` 或 `index.html`）
3. 怎樣算成功（例如開 https://sf9600-glitch.github.io/game-timer/ 並 **Cmd+Shift+R**）

## 部署

- 存上述三檔會觸發 push → **GitHub Pages** 約 1–3 分鐘更新。
- 用戶說「沒變化」時，先提醒：已存檔、等 deploy、強制重新整理；再查是否載入 `css/app.css` / `js/app.js`（非快取舊版）。

## 延續對話

- 同一功能微調：**在同一對話繼續**，不要為了小改重讀全專案。
- 用戶附截圖時，以圖為準，少問重複描述。
